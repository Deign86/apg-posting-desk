# Runtime deps for the Vercel serverless function (api/index.py).
# Vercel's Python runtime installs the requirements.txt closest to the handler,
# so this file governs what gets bundled into the function. Keep it to the
# minimal runtime set — dev/test/optional deps would blow the 225 MB function
# size limit (see vercel.json functions.excludeFiles and the deploy logs).
#
# For local development, install the full set from the repo-root requirements.txt.
fastapi>=0.115.0
openai>=1.55.0
Pillow>=11.0.0
PyPDF2>=3.0.1
python-docx>=1.1.2
python-dotenv>=1.0.1
PyYAML>=6.0.2
requests>=2.32.3
