# Runtime deps for the Vercel serverless function AND local dev.
# Vercel's Python runtime installs this file into the function bundle, so it MUST
# stay minimal — the 225 MB function-size limit was exceeded (235 MB) when the
# dev/optional deps below lived here. Dev/test/optional deps now live in
# requirements-dev.txt (installed separately for local work, never bundled).
fastapi>=0.115.0
openai>=1.55.0
Pillow>=11.0.0
PyPDF2>=3.0.1
python-docx>=1.1.2
python-dotenv>=1.0.1
PyYAML>=6.0.2
requests>=2.32.3
google-api-python-client>=2.150.0
google-auth>=2.35.0
google-auth-httplib2>=0.2.0

